The wrapping setting on the x-axis (horizontal). By default this is 1 (map wraps at the left-right edges). If you set this value to 0 then the x-axis does not wrap. Combined with the y wrap setting you can create flat maps, doughnut maps or maps that wrap left-right or top-bottom.

{{Civ4_World_Builder}}